# Django Settings
SECRET_KEY=your-secret-key-here-for-testing
ALLOWED_HOSTS=localhost,127.0.0.1
DEBUG=True
DJANGO_SETTINGS_MODULE=clinic_project.settings

# Database Configuration (MySQL)
DB_ENGINE=django.db.backends.mysql
DB_NAME=clinic_db
DB_USER=root
DB_PASSWORD=Jenkins_13
DB_HOST=localhost
DB_PORT=3306

# Email Configuration (optional - defaults to console backend for testing)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=
EMAIL_PORT=
EMAIL_USE_TLS=False
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=noreply@cliniccare.local
